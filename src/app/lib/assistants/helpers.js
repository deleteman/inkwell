

export function feedbackCategories() {
    return `"re-write with the new proposed text": when there is a way to re-write the text that sounds better
            "can be improved with suggestion": when the text can be improved slightly by a simple suggestion
            "wrong-theme": when the text doens't follow the provided theme/genre
            "too-cliche": when the text uses an overly used frase`
}